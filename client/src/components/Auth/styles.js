import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  btn: {
    "&:hover": {
      textDecoration: "none",
    },
    color: "#FFFFFF",
    margin: 30,
    fontSize: 25,
  },
}));
